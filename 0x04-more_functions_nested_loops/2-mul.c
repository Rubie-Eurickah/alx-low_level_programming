#include "main.h"

/**
 * mul - Multiplies two numbers
 * @a: The first value to multiply
 * @b: The second value to multiply by the first one
 * Return: The result obtained from the multiplication
 */

int mul(int a, int b)
{

	return (a * b);
}
